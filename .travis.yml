sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sreenivas-git/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sreenivas-git/react-test npm test

after_success:
  - docker build -t sreenivas-git/multi-client ./client
  - docker build -t sreenivas-git/multi-nginx ./nginx
  - docker build -t sreenivas-git/multi-server ./server
  - docker build -t sreenivas-git/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sreenivas-git/multi-client
  - docker push sreenivas-git/multi-nginx
  - docker push sreenivas-git/multi-server
  - docker push sreenivas-git/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'ap-south-1'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-ap-south-1-874079294750'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
